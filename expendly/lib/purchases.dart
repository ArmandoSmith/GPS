import 'package:flutter/material.dart';

class PurchasesScreen extends StatelessWidget {
  static const String routeName = "/purchases";

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      body: new Text("Pendiente"),
    );//Scaffold
  }
}